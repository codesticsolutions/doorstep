name: Generate Sitemap and Robots

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Determine Site Base URL (Custom Domain)
        id: url_calculation
        run: |
          BASE_URL="https://doorstepsupport.com/"
          echo "BASE_URL=${BASE_URL}" >> $GITHUB_ENV
          echo "Site custom domain base URL set to: $BASE_URL"

      - name: Run Sitemap Generation Script
        run: python generate_sitemap.py
        env:
          SITE_BASE_URL: ${{ env.BASE_URL }}

      - name: Commit and Push sitemap.xml and robots.txt
        uses: EndBug/add-and-commit@v9
        with:
          add: 'sitemap.xml robots.txt'
          message: 'Automated: Generate sitemap.xml and robots.txt'
          token: ${{ github.token }}
          committer_name: Github Actions Bot
          committer_email: actions@github.com
          author_name: Github Actions Bot
          author_email: actions@github.com